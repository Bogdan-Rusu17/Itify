apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-db-service-to-postgres-users
  namespace: itify
spec:
  podSelector:
    matchLabels:
      app: postgres-users
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: db-service
